# OGen (**O**penAPI **Gen**erator)
Reusable API client generator

The tool itself is created in Python, but it is generated using a template engine (Jinja2), so it is possible to generate clients in other languages.
